{
  "mission": {
    "statement": "Ship a restricted-access, invite-only CV-analysis web app that runs an agentic (LangGraph) workflow over the in-repo Base Roadmap via Qdrant RAG, and returns a personalized roadmap that is a strict subset of the Base Roadmap, deployed on free/starter-tier hosting (Vercel + Railway + Qdrant Cloud + Neon).",
    "terminal_signal": "End-to-end manual smoke: admin invites a user; user signs in on Vercel; submits CV + description; watches streamed agent progress (SSE); receives Level Resume, Compatibility Score (0-100), and a Personalized Roadmap whose every node_id resolves in the canonical RoadmapIndex. `make test` (lint + typecheck + unit) is green on CI."
  },
  "goals": [
    {
      "goal_id": "G1",
      "title": "Monorepo scaffolding & tooling",
      "rationale": "Establishes the workspace boundaries and shared orchestration that every later component depends on. Must build before any code component is added.",
      "sdd_reference": "docs/sdd/architecture.md#2-monorepo-structure",
      "completion_signal": "`pnpm install && make install` resolves; empty `apps/web`, `apps/api`, `packages/shared-types` exist; `make help` lists targets."
    },
    {
      "goal_id": "G2",
      "title": "Frontend foundation (Next.js responsive shell)",
      "rationale": "Provides the responsive authenticated shell and Auth.js wiring that all product surfaces mount into.",
      "sdd_reference": "docs/sdd/architecture.md#4-frontend-c-fe",
      "completion_signal": "`pnpm --filter web dev` boots Next.js; `/sign-in` renders responsive layout on mobile + desktop; root `/` redirects to `/sign-in` when unauthenticated."
    },
    {
      "goal_id": "G3",
      "title": "Backend FastAPI skeleton",
      "rationale": "Creates the app factory, settings, DTOs and shared TS DTOs that the auth, RAG and agent layers all plug into.",
      "sdd_reference": "docs/sdd/architecture.md#6-backend-fastapi-c-api",
      "completion_signal": "`uvicorn apps.api.src.app.main:app` boots; `GET /healthz` returns 200; CORS + structured logging active; shared-types package exports DTOs."
    },
    {
      "goal_id": "G4",
      "title": "Auth & relational persistence layer",
      "rationale": "Enables restricted access: invite-only users in Neon, Auth.js credentials/JWT on the web, and FastAPI JWT verification so the API can trust the session.",
      "sdd_reference": "docs/sdd/architecture.md#5-auth-subsystem-c-auth",
      "completion_signal": "Create-user CLI inserts a row in Neon; the user can sign in on the web; a request to a FastAPI protected stub with a valid Auth.js JWT passes and without it returns 401."
    },
    {
      "goal_id": "G5",
      "title": "Base Roadmap data layer + RAG (Qdrant)",
      "rationale": "Loads the canonical Base Roadmap, exposes the strict-subset validator, embeds nodes into Qdrant idempotently at startup, and provides the hybrid retriever used by the agent.",
      "sdd_reference": "docs/sdd/architecture.md#7-base-roadmap-data-layer-c-data",
      "completion_signal": "On boot, app seed logs 'collection populated: N points'; `RoadmapIndex.is_valid_subset([...])` returns True for real node_ids and False for fake ones; retriever returns ranked nodes for a sample query."
    },
    {
      "goal_id": "G6",
      "title": "Agentic LangGraph layer",
      "rationale": "Implements the analysis pipeline: ingest → strip → analyze → compare → level_guess → roadmap_select with the strict-subset guardrail enforced at the final node.",
      "sdd_reference": "docs/sdd/architecture.md#9-agentic-layer-langgraph-c-agent",
      "completion_signal": "Given a sample CV text + description, the graph streams progress events and returns a structured output whose `personalized_roadmap` is a strict subset of the Base Roadmap; an adversarial input is rejected by the validator with a hard error."
    },
    {
      "goal_id": "G7",
      "title": "Integration, product surfaces & deployment",
      "rationale": "Wires the annotated endpoint and SSE stream into FastAPI, persists analysis records, builds the three frontend surfaces, and adds container + Vercel/Neon/Qdrant deployment config and docs.",
      "sdd_reference": "docs/sdd/architecture.md#11-deployment-c-deploy",
      "completion_signal": "Manual smoke matches the Mission terminal_signal; `apps/api/Dockerfile` builds; `apps/web/vercel.json` valid; `.env.example` lists every required var; README documents the deploy."
    }
  ],
  "tasks": [
    {
      "task_id": "T1.1",
      "goal_id": "G1",
      "title": "Create root pnpm workspace skeleton",
      "rationale": "Defines workspace boundaries and ignores so later packages can be added under known top-level dirs.",
      "sdd_reference": "docs/sdd/architecture.md#2-monorepo-structure",
      "files": ["package.json", "pnpm-workspace.yaml", ".gitignore"],
      "blocked_by": [],
      "parallel_group": "P1a",
      "acceptance_test": "`pnpm -v` resolves; `pnpm-workspace.yaml` lists apps/* and packages/*; .gitignore covers node_modules, .venv, __pycache__, .next.",
      "sdd_snippet": "Monorepo Structure: pnpm workspaces for JS, Poetry/uv for Python, root Makefile. Top-level: apps/{web,api}, packages/shared-types, docs, .context."
    },
    {
      "task_id": "T1.2",
      "goal_id": "G1",
      "title": "Create apps/api Python project skeleton (uv/Poetry)",
      "rationale": "Backend package layout that FastAPI, Roadmap, RAG and Agent components will populate.",
      "sdd_reference": "docs/sdd/architecture.md#2-monorepo-structure",
      "files": ["apps/api/pyproject.toml", "apps/api/.python-version", "apps/api/src/app/__init__.py"],
      "blocked_by": ["T1.1"],
      "parallel_group": "P1b",
      "acceptance_test": "`cd apps/api && uv sync` (or poetry install) succeeds; `python -c 'import app'` from apps/api/src resolves.",
      "sdd_snippet": "apps/api: FastAPI + LangGraph (Python, Poetry/uv). Allowed deps: FastAPI, Pydantic v2, LangGraph, qdrant-client, asyncpg. No Nx."
    },
    {
      "task_id": "T1.3",
      "goal_id": "G1",
      "title": "Create root Makefile and README",
      "rationale": "Single entry point for cross-ecosystem install/dev/test/lint/seed so contributors never hand-compose commands.",
      "sdd_reference": "docs/sdd/architecture.md#2-monorepo-structure",
      "files": ["Makefile", "README.md"],
      "blocked_by": ["T1.1"],
      "parallel_group": "P1b",
      "acceptance_test": "`make help` prints install/dev/test/lint/seed targets; targets reference apps/web and apps/api.",
      "sdd_snippet": "Root Makefile for cross-ecosystem orchestration: install/dev/test/lint/seed. Tooling decision locked in ADR-001."
    },
    {
      "task_id": "T1.4",
      "goal_id": "G1",
      "title": "Create packages/shared-types stub package",
      "rationale": "Frontend/backend DTO synchronization point declared up front so later tasks add to it rather than inventing divergent types.",
      "sdd_reference": "docs/sdd/architecture.md#12-architectural-patterns",
      "files": ["packages/shared-types/package.json", "packages/shared-types/index.ts", "packages/shared-types/tsconfig.json"],
      "blocked_by": ["T1.1"],
      "parallel_group": "P1b",
      "acceptance_test": "`pnpm --filter shared-types build` succeeds exporting an empty `index.ts`.",
      "sdd_snippet": "DTO synchronization: packages/shared-types is generated/edited in lockstep with the FastAPI Pydantic schemas (manual keep-in-sync for MVP)."
    },
    {
      "task_id": "T2.1",
      "goal_id": "G2",
      "title": "Scaffold apps/web (Next.js App Router + TS + Tailwind)",
      "rationale": "Baseline Next.js app the frontend shell and product surfaces mount into.",
      "sdd_reference": "docs/sdd/architecture.md#4-frontend-c-fe",
      "files": ["apps/web/package.json", "apps/web/tsconfig.json", "apps/web/tailwind.config.ts"],
      "blocked_by": ["T1.1"],
      "parallel_group": "P2a",
      "acceptance_test": "`pnpm --filter web dev` boots; default Next.js route renders at localhost:3000.",
      "sdd_snippet": "Frontend stack: Next.js App Router, TypeScript strict, Tailwind CSS, shadcn/ui (Radix). Auth.js v5 for auth."
    },
    {
      "task_id": "T2.2",
      "goal_id": "G2",
      "title": "Add shadcn/ui, theme tokens and responsive layout shell",
      "rationale": "Establishes the responsive (mobile-first) shell all authenticated pages render within, satisfying the phone+desktop UI constraint.",
      "sdd_reference": "docs/sdd/architecture.md#4-frontend-c-fe",
      "files": ["apps/web/app/layout.tsx", "apps/web/components/shell.tsx", "apps/web/app/globals.css"],
      "blocked_by": ["T2.1"],
      "parallel_group": "P2b",
      "acceptance_test": "Shell renders header + nav + main across mobile (≤640px) and desktop widths via DevTools; Tailwind theme tokens resolve.",
      "sdd_snippet": "Responsive shell: <Header/>, <Nav/> (mobile drawer), <Main/>. Breakpoints; mobile-first. Design system tokens: color, spacing, typography via Tailwind theme + shadcn primitives."
    },
    {
      "task_id": "T2.3",
      "goal_id": "G2",
      "title": "Add Auth.js v5 base config + middleware stub",
      "rationale": "Wires the auth surface that G4 will back with a real credentials provider, and protects the app group now so later surfaces are secure by default.",
      "sdd_reference": "docs/sdd/architecture.md#5-auth-subsystem-c-auth",
      "files": ["apps/web/auth.ts", "apps/web/middleware.ts", "apps/web/app/sign-in/page.tsx"],
      "blocked_by": ["T2.1"],
      "parallel_group": "P2b",
      "acceptance_test": "Visiting `/` while signed-out redirects to `/sign-in`; sign-in page renders (form non-functional in this task).",
      "sdd_snippet": "Provider: Auth.js v5 credentials. Public routes: only `/sign-in`. No self-signup. Session: JWT in HttpOnly cookies."
    },
    {
      "task_id": "T2.4",
      "goal_id": "G2",
      "title": "Add protected route group + landing page",
      "rationale": "Holds the three product surfaces added in G7; demonstrates the auth gate end-to-end in the frontend.",
      "sdd_reference": "docs/sdd/architecture.md#4-frontend-c-fe",
      "files": ["apps/web/app/(app)/layout.tsx", "apps/web/app/(app)/page.tsx"],
      "blocked_by": ["T2.2"],
      "parallel_group": null,
      "acceptance_test": "Signed-in state shows the app shell; signed-out state bounces to `/sign-in`.",
      "sdd_snippet": "Route groups: /sign-in (public); (app)/ protected group containing / (analyze surface) and /analyze/:id (progress + results surfaces)."
    },
    {
      "task_id": "T3.1",
      "goal_id": "G3",
      "title": "Add FastAPI app factory + middleware (CORS, logging, request-id)",
      "rationale": "The app instance, middleware, lifespan seam and healthcheck every later module attaches to.",
      "sdd_reference": "docs/sdd/architecture.md#6-backend-fastapi-c-api",
      "files": ["apps/api/src/app/main.py", "apps/api/src/app/middleware.py", "apps/api/src/app/__init__.py"],
      "blocked_by": ["T1.2"],
      "parallel_group": "P3a",
      "acceptance_test": "`uvicorn apps.api.src.app.main:app --reload` boots; `GET /healthz` returns 200; a request logs a JSON line with request-id.",
      "sdd_snippet": "App factory create_app(). Middleware: CORS allowlist (Vercel domain + localhost), structured JSON logging with request-id. Endpoints: GET /healthz (unauthenticated liveness probe)."
    },
    {
      "task_id": "T3.2",
      "goal_id": "G3",
      "title": "Add pydantic-settings config",
      "rationale": "Single settings module auth, RAG and agent layers import from; env-var names match the SDD deploy table.",
      "sdd_reference": "docs/sdd/architecture.md#6-backend-fastapi-c-api",
      "files": ["apps/api/src/app/config.py"],
      "blocked_by": ["T1.2"],
      "parallel_group": "P3a",
      "acceptance_test": "Loading settings from a populated .env exposes QDRANT_URL, QDRANT_API_KEY, DATABASE_URL, LLM_API_KEY, EMBEDDING_MODEL, AUTHJS_JWT_SECRET, CORS_ALLOW, BASE_ROADMAP_PATH.",
      "sdd_snippet": "Settings: pydantic-settings loading env vars (Qdrant URL/key, Neon DSN, Auth.js secret, embedding model, LLM provider/key)."
    },
    {
      "task_id": "T3.3",
      "goal_id": "G3",
      "title": "Add Pydantic v2 backend DTOs",
      "rationale": "Authoritative request/response schemas the endpoint (T7.1) and the agent output (T6.5) share.",
      "sdd_reference": "docs/sdd/architecture.md#6-backend-fastapi-c-api",
      "files": ["apps/api/src/app/schemas/__init__.py", "apps/api/src/app/schemas/analyze.py"],
      "blocked_by": ["T1.2"],
      "parallel_group": "P3a",
      "acceptance_test": "Imports resolve; `AnalyzeRequest`, `AnalyzeResponse`, `RoadmapNode`, `LevelResume`, `AgentProgressEvent` validate sample JSON.",
      "sdd_snippet": "Pydantic v2 DTOs: AnalyzeRequest, AnalyzeResponse, RoadmapNode, LevelResume, AgentProgressEvent."
    },
    {
      "task_id": "T3.4",
      "goal_id": "G3",
      "title": "Mirror backend DTOs into packages/shared-types",
      "rationale": "Locks the frontend/backend contract at TypeScript; G7 surfaces import these types instead of redefining them.",
      "sdd_reference": "docs/sdd/architecture.md#12-architectural-patterns",
      "files": ["packages/shared-types/index.ts"],
      "blocked_by": ["T1.4", "T3.3"],
      "parallel_group": null,
      "acceptance_test": "`pnpm --filter shared-types build` exports TS interfaces matching the Pydantic schemas in T3.3.",
      "sdd_snippet": "packages/shared-types is generated/edited in lockstep with the FastAPI Pydantic schemas (manual keep-in-sync for MVP; no codegen)."
    },
    {
      "task_id": "T4.1",
      "goal_id": "G4",
      "title": "Add Neon connection + Drizzle schema + initial migration",
      "rationale": "Canonical user/analysis schema shared by Auth.js and (later) FastAPI history persistence; one schema source.",
      "sdd_reference": "docs/sdd/architecture.md#10-relational-store-neon-postgres-c-db",
      "files": ["packages/db/package.json", "packages/db/schema.ts", "packages/db/drizzle.config.ts"],
      "blocked_by": ["T1.1"],
      "parallel_group": "P4a",
      "acceptance_test": "`pnpm --filter db migrate` applies `0001_init.sql` against a Neon branch; `users` and `analyses` tables exist with columns per SDD.",
      "sdd_snippet": "Tables: users(id UUID PK, email UNIQUE, password_hash, created_at, is_admin BOOL); analyses(id UUID PK, user_id FK, request JSONB, result JSONB, status ENUM[running,done,failed], created_at, completed_at). Migrations via Drizzle ORM in packages/db."
    },
    {
      "task_id": "T4.2",
      "goal_id": "G4",
      "title": "Wire Auth.js credentials provider (argon2 + JWT + Drizzle)",
      "rationale": "Makes the sign-in page functional and issues the JWT the FastAPI side verifies.",
      "sdd_reference": "docs/sdd/architecture.md#5-auth-subsystem-c-auth",
      "files": ["apps/web/lib/auth-db.ts", "apps/web/lib/password.ts", "apps/web/auth.ts"],
      "blocked_by": ["T4.1"],
      "parallel_group": "P4b",
      "acceptance_test": "With a seeded user, sign-in at `/sign-in` succeeds and sets an HttpOnly JWT cookie; wrong password fails.",
      "sdd_snippet": "Credentials provider, argon2 hashing, JWT session cookies (short-lived access + refresh). Backend verifies the Auth.js JWT."
    },
    {
      "task_id": "T4.3",
      "goal_id": "G4",
      "title": "Add invite-only create-user CLI script",
      "rationale": "Operationalizes the no-self-signup rule: an admin creates users; no public endpoint exists.",
      "sdd_reference": "docs/sdd/architecture.md#5-auth-subsystem-c-auth",
      "files": ["packages/db/scripts/create-user.ts", "packages/db/scripts/README.md"],
      "blocked_by": ["T4.1"],
      "parallel_group": "P4b",
      "acceptance_test": "`pnpm --filter db exec scripts/create-user.ts <email>` inserts a user row with an argon2 hash from a prompted password; a second run with same email is rejected.",
      "sdd_snippet": "Invite flow: admin creates users via a CLI/script backed by Neon Postgres (admin token readable from env; no admin UI required for MVP)."
    },
    {
      "task_id": "T4.4",
      "goal_id": "G4",
      "title": "Add FastAPI JWT verification dependency",
      "rationale": "Lets protected FastAPI routes trust the Auth.js session by validating the JWT issued in T4.2.",
      "sdd_reference": "docs/sdd/architecture.md#5-auth-subsystem-c-auth",
      "files": ["apps/api/src/app/auth.py"],
      "blocked_by": ["T3.2"],
      "parallel_group": "P4b",
      "acceptance_test": "A FastAPI test route guarded by the dependency returns 401 without the JWT and 200 with a valid Auth.js-issued JWT; user_id is attached to request state.",
      "sdd_snippet": "Backend verification (C-API): FastAPI dependency decodes & verifies the Auth.js JWT (shared secret / JWKS), attaching user_id to the request state. Protected endpoints reject unauthenticated requests with 401."
    },
    {
      "task_id": "T5.1",
      "goal_id": "G5",
      "title": "Add Base Roadmap Pydantic models, loader and RoadmapIndex (incl. validator)",
      "rationale": "Single in-memory source of truth over docs/archives JSONs; the validator is the ADR-008 guardrail hook.",
      "sdd_reference": "docs/sdd/architecture.md#7-base-roadmap-data-layer-c-data",
      "files": ["apps/api/src/roadmap/models.py", "apps/api/src/roadmap/loader.py", "apps/api/src/roadmap/index.py"],
      "blocked_by": ["T3.2"],
      "parallel_group": "P5a",
      "acceptance_test": "Loading parses all docs/archives/*.json; `RoadmapIndex.is_valid_subset(['valid-id'])` is True; `is_valid_subset(['fake-id'])` is False; reverse lookup by role+level+category returns expected nodes.",
      "sdd_snippet": "Pydantic models: RoadmapFile, RoadmapNode (id, name, type, category, level, importance, aliases, requirements_by_level, content_guidance, interview). Loader builds RoadmapIndex keyed by node_id with reverse lookup by role+level+category. Validator is_valid_subset(node_ids) is the single source of truth for the strict-subset rule (ADR-008)."
    },
    {
      "task_id": "T5.2",
      "goal_id": "G5",
      "title": "Add Qdrant client + embedding service interface",
      "rationale": "Connection + embedding abstraction the seeder and retriever depend on; lets the embed model be swapped later.",
      "sdd_reference": "docs/sdd/architecture.md#8-rag-qdrant-c-rag",
      "files": ["apps/api/src/rag/qdrant_client.py", "apps/api/src/rag/embeddings.py"],
      "blocked_by": ["T3.2"],
      "parallel_group": "P5a",
      "acceptance_test": "Unit test mocks Qdrant Cloud and embedding model; the embedding service returns fixed-length vectors; the client exposes async upsert/search wrappers.",
      "sdd_snippet": "Client: qdrant-client async, pointing at Qdrant Cloud. Embeddings: text-embedding model (configurable; default text-embedding-3-small or local fallback) behind an interface so model can change."
    },
    {
      "task_id": "T5.3",
      "goal_id": "G5",
      "title": "Add idempotent startup seeder + lifespan wiring",
      "rationale": "Implements ADR-006: collections are populated from Base Roadmap on boot iff empty, so a re-deploy cannot drift.",
      "sdd_reference": "docs/sdd/architecture.md#8-rag-qdrant-c-rag",
      "files": ["apps/api/src/rag/seeder.py", "apps/api/src/app/main.py"],
      "blocked_by": ["T5.1", "T5.2"],
      "parallel_group": "P5b",
      "acceptance_test": "On a fresh collection, booting the app embeds and upserts every Base Roadmap node with payload {role, level, node_id, category, importance, name, description, content_guidance}; a second boot is a no-op (point count matches).",
      "sdd_snippet": "Startup seeder (ADR-006): idempotent — if collection point count == 0, parse Base Roadmap via C-DATA, embed each node's name+description+topics, upsert. Otherwise skip. Collection roadmap_nodes with payload {role, level, node_id, category, importance, name, description, content_guidance}."
    },
    {
      "task_id": "T5.4",
      "goal_id": "G5",
      "title": "Add hybrid retriever with role/level filters",
      "rationale": "Returns ranked Base Roadmap nodes for the agent's compare node; constrained to roles/levels via metadata filters.",
      "sdd_reference": "docs/sdd/architecture.md#8-rag-qdrant-c-rag",
      "files": ["apps/api/src/rag/retriever.py"],
      "blocked_by": ["T5.1", "T5.2"],
      "parallel_group": "P5b",
      "acceptance_test": "A test query with filters `{role: software_engineer, level: senior}` returns only matching node_ids, ranked by relevance, top-k configurable.",
      "sdd_snippet": "Retriever: hybrid search (dense vector + sparse keyword/alias match) with filters {role, level}; returns candidate RoadmapNodes ranked by relevance. Tunable top-k."
    },
    {
      "task_id": "T6.1",
      "goal_id": "G6",
      "title": "Add AgentState schema + ingest + strip nodes",
      "rationale": "Defines the shared graph state and turns raw CV bytes + description into clean text all later nodes consume.",
      "sdd_reference": "docs/sdd/architecture.md#9-agentic-layer-langgraph-c-agent",
      "files": ["apps/api/src/agent/state.py", "apps/api/src/agent/nodes/ingest.py"],
      "blocked_by": ["T3.3"],
      "parallel_group": null,
      "acceptance_test": "Given bytes for a small PDF/DOCX/TXT + text description, the strip node populates `raw_cv_text` and `raw_description` on state; ingest asserts the file is ephemeral.",
      "sdd_snippet": "State schema: AgentState { user_id, raw_cv_text, raw_description, extracted_skills, matched_nodes, level_estimate, compatibility_score, personalized_roadmap, errors }. Node ingest stores bytes ephemerally; node strip extracts clean text from PDF/DOCX/TXT."
    },
    {
      "task_id": "T6.2",
      "goal_id": "G6",
      "title": "Add analyze node (structured skill extraction)",
      "rationale": "Maps the user's CV/description into structured skills/tech/experience for comparison.",
      "sdd_reference": "docs/sdd/architecture.md#9-agentic-layer-langgraph-c-agent",
      "files": ["apps/api/src/agent/nodes/analyze.py"],
      "blocked_by": ["T6.1"],
      "parallel_group": "P6a",
      "acceptance_test": "On a sample CV, the node returns a structured `extracted_skills` object via a typed LLM tool-call; malformed output raises a validation error rather than passing silently.",
      "sdd_snippet": "analyze — LLM tool-call with structured schema: extract skills, technologies, years of experience, domain areas."
    },
    {
      "task_id": "T6.3",
      "goal_id": "G6",
      "title": "Add compare node (retriever-backed gap analysis)",
      "rationale": "Bridges agent and RAG: identifies which Base Roadmap nodes are covered vs gap areas.",
      "sdd_reference": "docs/sdd/architecture.md#9-agentic-layer-langgraph-c-agent",
      "files": ["apps/api/src/agent/nodes/compare.py"],
      "blocked_by": ["T5.4", "T6.1"],
      "parallel_group": "P6a",
      "acceptance_test": "Given mock `extracted_skills`, the node calls the retriever, classifies coverage, and writes `matched_nodes` + gap list onto state.",
      "sdd_snippet": "compare — call C-RAG retriever for relevant Base Roadmap nodes matching extracted skills; identify gaps."
    },
    {
      "task_id": "T6.4",
      "goal_id": "G6",
      "title": "Add level_guess node (seniority + compatibility score)",
      "rationale": "Produces the user-facing Level Resume inputs: seniority estimate, trajectory narrative, and 0-100 score.",
      "sdd_reference": "docs/sdd/architecture.md#9-agentic-layer-langgraph-c-agent",
      "files": ["apps/api/src/agent/nodes/level_guess.py"],
      "blocked_by": ["T6.1"],
      "parallel_group": "P6a",
      "acceptance_test": "Given mock matched/gap nodes, the node returns one of {junior, mid, senior, staff}, a narrative string, and a `compatibility_score` in [0,100].",
      "sdd_snippet": "level_guess — estimate seniority (junior/mid/senior/staff) + trajectory narrative; compute compatibility_score (0-100)."
    },
    {
      "task_id": "T6.5",
      "goal_id": "G6",
      "title": "Add roadmap_select node + strict-subset guardrail + graph wiring + SSE mapping",
      "rationale": "Final node: selects the personalized subset, validates every id against RoadmapIndex, wires all nodes into the LangGraph, and emits progress events consumable by SSE.",
      "sdd_reference": "docs/sdd/architecture.md#9-agentic-layer-langgraph-c-agent",
      "files": ["apps/api/src/agent/nodes/roadmap_select.py", "apps/api/src/agent/graph.py", "apps/api/src/agent/streaming.py"],
      "blocked_by": ["T6.2", "T6.3", "T6.4", "T5.1"],
      "parallel_group": null,
      "acceptance_test": "Running `graph.run(...)` on a sample CV streams one progress event per node and returns a result whose `personalized_roadmap` nodes ALL pass `RoadmapIndex.is_valid_subset`; injecting a hallucinated id forces a re-plan then a hard error.",
      "sdd_snippet": "roadmap_select — select subset of nodes addressing gaps/next steps; emit personalized_roadmap. Strict-subset guardrail (ADR-008): final node validates every selected node_id via RoadmapIndex.is_valid_subset. Invalid → re-plan once with a corrective prompt, then hard-error. NEVER emit hallucinated nodes. Stop criteria: max iterations (e.g., 6), structured-output validator passes. Streaming: LangGraph state events map to SSE AgentProgressEvent payloads."
    },
    {
      "task_id": "T7.1",
      "goal_id": "G7",
      "title": "Add FastAPI POST /analyze + SSE stream endpoint and history persistence",
      "rationale": "Turns the LangGraph runner into the public, auth-protected, persisting API the frontend consumes.",
      "sdd_reference": "docs/sdd/architecture.md#6-backend-fastapi-c-api",
      "files": ["apps/api/src/app/api/analyze.py", "apps/api/src/app/main.py", "apps/api/src/app/storage/analyses.py"],
      "blocked_by": ["T6.5", "T4.4"],
      "parallel_group": "P7a",
      "acceptance_test": "Calling `POST /analyze` (authed) returns 201 + analysis_id; `GET /analyze/{id}/events` streams progress then final result via SSE; a row in `analyses` transitions running→done; an unauthed call returns 401.",
      "sdd_snippet": "Endpoints: POST /analyze (multipart DTO §6, echoes 201 with analysis_id, begins streaming agent progress); GET /analyze/{id}/events (SSE stream delivering node-progress events + final result DTO). FastAPI verifies the Auth.js JWT; protected endpoints reject unauthenticated requests with 401. Persist analysis record (input + output + node IDs) to Neon Postgres."
    },
    {
      "task_id": "T7.2",
      "goal_id": "G7",
      "title": "Add frontend input form surface",
      "rationale": "First product surface: collects the DTO and posts it to the API gateway.",
      "sdd_reference": "docs/sdd/architecture.md#4-frontend-c-fe",
      "files": ["apps/web/app/(app)/page.tsx", "apps/web/components/analyze-form.tsx", "apps/web/lib/api.ts"],
      "blocked_by": ["T2.4", "T3.4", "T7.1"],
      "parallel_group": "P7b",
      "acceptance_test": "Submitting the form (authed) posts multipart to `${NEXT_PUBLIC_API_BASE_URL}/analyze`, displays the returned analysis_id, and navigates to `/analyze/{id}`; mobile and desktop layouts both render correctly.",
      "sdd_snippet": "Three product surfaces: (a) input form (name/phone/email/description/CV file upload) → POST to /analyze. Typed fetcher hitting FastAPI SSE endpoint; DTOs synchronized from packages/shared-types."
    },
    {
      "task_id": "T7.3",
      "goal_id": "G7",
      "title": "Add frontend progress + results surfaces (SSE consumer)",
      "rationale": "Second and third surfaces: drives the streaming progress UI and renders the verified result.",
      "sdd_reference": "docs/sdd/architecture.md#4-frontend-c-fe",
      "files": ["apps/web/app/(app)/analyze/[id]/page.tsx", "apps/web/components/progress-panel.tsx", "apps/web/components/results-view.tsx"],
      "blocked_by": ["T2.4", "T3.4", "T7.1"],
      "parallel_group": "P7b",
      "acceptance_test": "A visited `/analyze/{id}` subscribes to the SSE stream, renders each AgentProgressEvent, then renders Level Resume, Compatibility Score and Personalized Roadmap list on the terminal event; responsive on mobile + desktop.",
      "sdd_snippet": "Surfaces: (b) streaming progress / loader, (c) results view (Level Resume, Compatibility Score, Personalized Roadmap). DTOs from packages/shared-types."
    },
    {
      "task_id": "T7.4",
      "goal_id": "G7",
      "title": "Add deployment config (Dockerfile, Vercel, .env.example) and README deploy section",
      "rationale": "Locks the ADR-002/ADR-007 hosting topology so the manual smoke can run end-to-end on real infra.",
      "sdd_reference": "docs/sdd/architecture.md#11-deployment-c-deploy",
      "files": ["apps/api/Dockerfile", "apps/web/vercel.json", ".env.example"],
      "blocked_by": ["T6.5"],
      "parallel_group": "P7a",
      "acceptance_test": "`docker build apps/api` succeeds and starts uvicorn; `vercel build` for apps/web validates; `.env.example` lists every var named in SDD §11; README deploy section documents Railway + Vercel + Qdrant + Neon steps.",
      "sdd_snippet": "Frontend: apps/web → Vercel (auto-detect Next.js). Env: NEXTAUTH_SECRET, NEXTAUTH_URL, AUTHJS_JWKS_URL/shared secret, NEXT_PUBLIC_API_BASE_URL. Backend: apps/api → Railway via Dockerfile (slim Python base, uv install, uvicorn entrypoint). Env: QDRANT_URL, QDRANT_API_KEY, DATABASE_URL, LLM_API_KEY, EMBEDDING_MODEL, AUTHJS_JWT_SECRET, CORS_ALLOW, BASE_ROADMAP_PATH."
    }
  ],
  "topological_order": [
    "T1.1",
    "T1.2",
    "T1.3",
    "T1.4",
    "T2.1",
    "T2.2",
    "T2.3",
    "T2.4",
    "T3.1",
    "T3.2",
    "T3.3",
    "T3.4",
    "T4.1",
    "T4.2",
    "T4.3",
    "T4.4",
    "T5.1",
    "T5.2",
    "T5.3",
    "T5.4",
    "T6.1",
    "T6.2",
    "T6.3",
    "T6.4",
    "T6.5",
    "T7.1",
    "T7.4",
    "T7.2",
    "T7.3"
  ],
  "validation_passed": true
}